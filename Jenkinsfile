

if (env.CHANGE_ID) {
//  if(pullRequest.head != )
}




pipeline {
	agent {
	  label 'master'
	}
	options { skipDefaultCheckout() }
        stages {
	stage("x") {
		steps {
		  sh 'set'
		  sh 'sleep 30'
		}
}
	}

}
